#include "main.h"
/**
 * _isupper - prototype check the code
 * @c: variable 
 * Return: 1 uppercase 0 not uppercase
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
