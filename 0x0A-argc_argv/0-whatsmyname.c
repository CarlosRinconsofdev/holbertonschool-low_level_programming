#include <stdio.h>
/**
 * main - check the code
 * @argc: count
 * @argv: value
 * Return: Always 0 (Succes)
 */
int main(int argc, char *argv[])
{
if (argc > 0)
printf("%s\n", argv[0]);
return (0);
}
